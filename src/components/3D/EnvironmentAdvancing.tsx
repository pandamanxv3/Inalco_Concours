EnvironmentAdvancing

// import { useMeshState } from '../../store/ContextBoard';
// import { initialConfig } from '../../store/initialConfig';
// import { useEffect } from 'react';
// import useInterfaceStore from '../../store/store';
// import gsap from 'gsap';

function EnvironmentAdvancing() {
	// const { maxZEnv, totalTimeAnimation } = initialConfig;
	// const { EnvRef: Env, CharRef, AnimalRef } = useMeshState().meshRefs;
	// const { state, resetExperience, setExperienceStarted, experienceStarted } = useInterfaceStore();

	// useEffect(() => {
	// 	if (state !== "base" && !experienceStarted) {
	// 		setExperienceStarted(true);
	// 		gsap.to(Env.current!.position, {
	// 			duration: totalTimeAnimation,
	// 			z: maxZEnv,
	// 			onComplete: () => {
	// 				resetExperience(Env.current!, CharRef.current!, AnimalRef.current!);
	// 			},
	// 		});
	// 	}
	// }, [state]);

	return (
		<>

		</>
	);
}

export default EnvironmentAdvancing;
